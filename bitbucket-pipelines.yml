pipelines:
    branches:
      main:
        - step:
            name: Build and Deploy
            script: 
              - git init
              - git add .
              - git commit -m "Deploy to Github"
              - git remote add origin https://github.com/single-developer/ecommerce-backend.git
              - git push -u origin main